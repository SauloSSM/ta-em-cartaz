spring:
  flyway:
    locations: classpath:db/migration,classpath:db/seed/demo
auth:
  cookies:
    secure: ${AUTH_COOKIES_SECURE:false}
  cors:
    allowed-origins:
      - https://allowed.example
      - https://ta-em-cartaz.vercel.app
  jwt:
    ttl: PT1H
