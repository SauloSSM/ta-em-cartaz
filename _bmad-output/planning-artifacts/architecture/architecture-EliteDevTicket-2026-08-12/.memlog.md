---
scope: EliteDevTicket platform
purpose: Build substrate for epics, stories, Codex and technical evaluation
altitude: feature
updated: 2026-08-12T21:24
---

- (direction) Coaching path; checkpoint only for load-bearing architecture decisions, trade-offs, Domain/security/consistency/API/UX impacts; trivial implementation details delegated.
- (direction) Only deliverable is terse ARCHITECTURE-SPINE.md for epics/stories, Codex and technical evaluation.
- (constraint) Inherit PDF, PRD approved, Domain v1.2, addendum and UX Behavioral Contract; no discovery reopening, Domain changes, optional promotion, silent features or LOW_AVAILABILITY threshold.
- (decision) ADOPTED paradigm: pragmatic Modular Monolith with light Ports and Adapters, package-by-feature; ports only at real boundaries/integrations/test seams, no ceremonial interfaces.
- (decision) ADOPTED ownership: events owns Event/TicketSector configuration; reservations owns Reservation lifecycle and exclusively consumes/returns availability due to hold/expiry; no inventory module.
- (decision) ADOPTED capacity mutation invariant: committed=capacity-availableQuantity; events accepts newCapacity>=committed and sets newAvailableQuantity=newCapacity-committed, never arbitrary stock adjustment.
- (decision) ADOPTED concurrency: PESSIMISTIC_WRITE on TicketSector for all capacity/availability mutation; DB constraints capacity>0, 0<=available<=capacity, price>=0 remain defense-in-depth.
- (decision) ADOPTED hold serialization: reservations locks Customer through an explicit auth module port/boundary, never auth JPA entity/repository; conscious MVP serialization trade-off.
- (decision) ADOPTED lock order Customer→Reservation→TicketSector; multiple sectors locked by ascending UUID; never sector then reservation/customer.
- (decision) ADOPTED Payment: lock/revalidate HOLDING+vigent Reservation before every attempt; APPROVED persists payment+CONFIRMED+exact tickets atomically; DECLINED persists only after valid hold and leaves Reservation unchanged.
- (decision) ADOPTED issuance DB guard: Ticket has ordinal/issuance key unique per Reservation, equivalent UNIQUE(reservation_id, ordinal).
- (decision) ADOPTED reservation idempotency: persistent no-TTL record unique Customer+Idempotency-Key with payload hash, reservationId, timestamps; same hash reconstructs replay, different hash IDEMPOTENCY_CONFLICT.
- (decision) ADOPTED auth: JWT in HttpOnly cookie, CSRF for SPA, Secure production, SameSite=Lax same logical origin, 8h configurable TTL aligned cookie/JWT, no refresh token; CSRF renewed after login/logout.
- (decision) ADOPTED pre-login intent: sessionStorage allowlist eventId, ticketSectorId, quantity, internal returnPath, createdAt; TTL 15m; no price/token/Reservation; backend revalidates all authoritative state.
- (decision) ADOPTED API: /api/v1, stable safe error envelope code/message/optional fieldErrors/traceId/timestamp; HTTP errors distinct from business outcomes Payment DECLINED and Gate VALID/INVALID/ALREADY_USED/WRONG_EVENT in response models.
- (decision) ADOPTED MVP ticket secrets: high-entropy validation/share tokens and crypto-random digit-friendly manualCode stored re-displayable with UNIQUE constraints; never predictable IDs or logs; database/backups treated sensitive.
- (direction) DEFERRED hardening/COULD: at-rest encryption plus digest lookup; in-memory single-instance rate limiting; append-only GateValidationAttempt. MVP uses atomic Ticket.usedAt/usedByGateUserId.
- (decision) ADOPTED QrDecoder port: one proven camera decode path for target browsers behind adapter; BarcodeDetector optional progressive enhancement, JS baseline/fallback permitted; manual always available; route exit stops tracks, hidden tab restores safely, decoded QR pauses until result/next without mandatory session recreation.
- (decision) ADOPTED timer contract: responses include serverNow/expiresAt/status; UI anchors syncedServerNow to monotonic performance.now elapsed; backend remains authority; reconcile load/refresh/visibility/payment/zero/errors.
- (decision) ADOPTED payment reconciliation: frontend generates UUID paymentAttemptId before deliberate command; backend processes/persists once and exposes query; same attempt reuse same ID, new attempt after DECLINED new ID, never auto-repeat lost command.
- (decision) ADOPTED Gate reconciliation/audit: frontend UUID validationAttemptId; backend persists attemptId, gate, selectedEvent, identifiable ticket, method, result, processedAt and secure input fingerprint; same compatible ID replays original result incl VALID, incompatible payload conflict; never stores raw QR/token/manual code.
- (decision) ADOPTED negative contract: expose availableQuantity only; no LOW_AVAILABILITY enum/field/threshold in backend or frontend.
- (decision) ADOPTED Ticketmaster adapter: backend-only CatalogProvider, total operation budget ~5s configurable incl max one transient retry; no generic 4xx retry; 429 explicit rate-limit/unavailable without long wait; no cache/circuit breaker/raw payload/secret exposure.
- (decision) ADOPTED expiration: serverNow>=expiresAt is semantically expired regardless persisted status; scheduler cleanup default 30s, small batches, one transaction each; all critical operations reconcile authoritatively.
- (decision) ADOPTED lazy expiry before new hold: query relevant expired HOLDING reservations for requested sector, lock Reservations ascending UUID then all involved TicketSectors ascending UUID, expire/return once, then validate availability; preserves canonical lock order and avoids false scarcity.
- (decision) ADOPTED observability: structured stdout logs, traceId, mandatory secret/path redaction, minimal public liveness/readiness without internals; DB/migrations readiness, Ticketmaster excluded; no metrics/tracing platform.
- (decision) ADOPTED environments: same logical origin with /api forwarding and no required extra proxy service; local/test/demo/prod profiles; demo HTTPS Secure cookies, external secrets and documented seeded accounts; Flyway schema/seeds owner, Hibernate validate.
- (version) VERIFIED seed: Java 21; Spring Boot 4.0.7 official release; React 19.2.x; TypeScript 5.x; Vite 7.3.x deliberate supported conservative line; PostgreSQL 17.x supported; exact patches cold-start then pom/lockfile authoritative.
- (decision) ADOPTED test floor: domain/API/adapter/E2E plus real PostgreSQL for locks/constraints/Flyway/concurrency; Testcontainers preferred, Compose PostgreSQL fallback, never in-memory evidence for concurrency.
- (decision) ADOPTED attempt idempotency: payment/validation attempts unique by attemptId with canonical fingerprint, atomic claim/process/result, compatible replay, incompatible conflict, no duplicate effect under concurrency; never persist raw Gate credential.
- (decision) ADOPTED Gate atomicity: ValidationAttempt claim/result and optional Ticket VALID→USED commit in same transaction; WRONG_EVENT checked before consumption and never mutates Ticket.
- (decision) ADOPTED JWT crypto: HS256 with external >=256-bit secret distinct per environment, required outside dev, never seeded/versioned; preserves HttpOnly/Secure published/SameSite=Lax/CSRF contract.
- (decision) ADOPTED manualCode: crypto-secure 10-char ambiguity-free Crockford-like Base32, displayed grouped; normalized case/separators, UNIQUE normalized value, never logged/audited complete.
- (decision) ADOPTED architecture tests in build, preferably ArchUnit: no cross-module internal imports, controller→repository, JPA entity in HTTP DTO, or dependency outside approved graph; avoid naming-only ceremony.
- (change) Reviewer direct fixes approved: correct UX source path, complete README/seeds, explicit cross-sector lazy expiry, remove catalog→DB, record compatible Node version, atomic ValidationAttempt/Ticket consumption.
- (decision) ADOPTED fixed hold: expiresAt=authoritative serverNow at creation + exactly 10 minutes; no pause, restart or extension; deterministic test required.
- (decision) ADOPTED fingerprint v1: Payment canonicalizes session customerId,reservationId,simulatedOutcome; Validation canonicalizes session gateUserId,selectedEventId,method,credential digest. MANUAL uses AD-13 normalization; QR preserves exact token semantics. No original credential/log/audit/response fingerprint.
- (decision) ADOPTED HTTP authority: versioned OpenAPI for MVP /api/v1 requests/responses/auth/errors/business outcomes; PRD/Domain remain behavior/invariant authority; Java DTOs and TS types conform with automated drift checks; frontend types may derive, no server/controller generation required.
- (decision) ADOPTED explicit verification: compatible replay, incompatible conflict, concurrent single effect, Gate original VALID replay, WRONG_EVENT no consume, exact 10-minute expiresAt.
- (decision) ADOPTED BCrypt: cost configurable per environment with explicit code/cold-start default for new hashes; tests verify auth and produced hash parameters/cost, never literal hash; seeded credentials only local/test/demo.
- (event) Third gate PASS: official lint 0, reconciliation 0 conflicts/findings, rubric 0 findings, adversarial/version review 0 material findings.
- (event) spine finalized
