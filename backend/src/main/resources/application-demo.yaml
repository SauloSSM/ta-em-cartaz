spring:
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
  flyway:
    locations: classpath:db/migration,classpath:db/seed/demo
auth:
  cookies:
    secure: ${AUTH_COOKIES_SECURE:true}
